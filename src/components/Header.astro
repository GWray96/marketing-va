---
import { SITE, CTA } from '../config';

const nav = [
  { label: 'Services', href: '/services' },
  { label: 'How it works', href: '/how-it-works' },
  { label: 'Pricing', href: '/pricing' },
  { label: 'Work', href: '/work' },
  { label: 'About', href: '/about' },
];
---

<header class="site-header">
  <div class="header-inner wrap">
    <a href="/" class="logo" aria-label={`${SITE.name} — home`}>
      Marketing&nbsp;Executive&nbsp;<span class="logo-va">VA</span>
    </a>

    <nav class="primary" aria-label="Main navigation">
      <button class="nav-toggle" aria-expanded="false" aria-controls="nav-list">
        <span class="nav-toggle-label">Menu</span>
      </button>
      <ul id="nav-list">
        {nav.map((item) => (
          <li><a href={item.href}>{item.label}</a></li>
        ))}
        <li class="nav-cta-item">
          <a href={SITE.booking.url} class="btn nav-cta" data-event="cta_click" data-event-loc="header">
            {CTA.directShort}
          </a>
        </li>
      </ul>
    </nav>
  </div>
</header>

<script>
  const toggle = document.querySelector('.nav-toggle');
  const list = document.getElementById('nav-list');
  toggle?.addEventListener('click', () => {
    const open = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!open));
    list?.classList.toggle('open', !open);
  });

  const header = document.querySelector('.site-header') as HTMLElement | null;
  if (header) {
    const THRESHOLD = 72;
    const onScroll = () => {
      header.classList.toggle('scrolled', window.scrollY > THRESHOLD);
    };
    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll();
  }
</script>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in srgb, var(--bg) 92%, transparent);
    backdrop-filter: blur(8px);
    border-bottom: 1px solid var(--tint);
    transition:
      background 0.35s ease,
      border-color 0.35s ease,
      padding-block 0.35s ease,
      backdrop-filter 0.35s ease;
  }

  .site-header.scrolled {
    background: transparent;
    border-bottom-color: transparent;
    backdrop-filter: none;
    padding-block: 8px;
    padding-inline: clamp(12px, 2vw, 20px);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    padding-block: 14px;
    border-radius: 0;
    border: 1px solid transparent;
    transition:
      background 0.35s cubic-bezier(0.4, 0, 0.2, 1),
      border-radius 0.35s cubic-bezier(0.4, 0, 0.2, 1),
      border-color 0.35s cubic-bezier(0.4, 0, 0.2, 1),
      box-shadow 0.35s cubic-bezier(0.4, 0, 0.2, 1),
      padding-block 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  }

  @media (min-width: 881px) {
    .site-header.scrolled .header-inner {
      background: color-mix(in srgb, var(--bg) 97%, transparent);
      backdrop-filter: blur(14px);
      border-radius: 9999px;
      border-color: var(--tint);
      padding-block: 8px;
      box-shadow:
        0 4px 24px rgba(28, 19, 9, 0.10),
        0 1px 4px rgba(28, 19, 9, 0.06);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .site-header,
    .header-inner {
      transition: none;
    }
  }

  .logo {
    font-weight: 700;
    font-size: 1.05rem;
    letter-spacing: -0.01em;
    color: var(--ink);
    text-decoration: none;
    white-space: nowrap;
  }

  .logo-va {
    color: var(--copper-deep);
  }

  nav.primary ul {
    display: flex;
    align-items: center;
    gap: 26px;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  nav.primary a:not(.btn) {
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--ink);
  }

  nav.primary a:not(.btn):hover {
    color: var(--copper-deep);
    text-decoration: underline;
    text-decoration-color: var(--teal);
    text-underline-offset: 5px;
  }

  .nav-cta {
    padding: 10px 20px;
    font-size: 0.9rem;
  }

  .nav-toggle {
    display: none;
    background: none;
    border: 2px solid var(--ink);
    border-radius: 6px;
    padding: 6px 12px;
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 0.85rem;
    color: var(--ink);
  }

  @media (max-width: 880px) {
    .nav-toggle {
      display: block;
    }

    nav.primary ul {
      display: none;
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      flex-direction: column;
      align-items: flex-start;
      gap: 0;
      background: var(--bg);
      border-bottom: 1px solid var(--tint);
      padding: 8px var(--pad-x) 20px;
    }

    nav.primary ul.open {
      display: flex;
    }

    nav.primary ul li {
      width: 100%;
    }

    nav.primary ul a:not(.btn) {
      display: block;
      padding: 12px 0;
      font-size: 1.05rem;
    }

    .nav-cta-item {
      margin-top: 10px;
    }
  }
</style>
